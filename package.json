{
  "scripts": {
    "start": "node --watch index.js",
    "pull": "npx prisma db pull && npx prisma generate",
    "prisma:generate": "prisma generate",
    "migrate": "npx prisma generate && npm run pull"
  },
  "dependencies": {
    "@prisma/client": "^6.2.1",
    "axios": "^1.7.9",
    "bcrypt": "^5.1.1",
    "express": "^4.21.2",
    "jose": "^5.9.6",
    "prisma": "^6.2.1"
  }
}
